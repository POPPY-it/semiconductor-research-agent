# 秋招项目技术方案：半导体行业研报 Agent（Semiconductor Research Agent）

> **对齐说明（2026-08）**：本文件为**原始施工图**，其中部分表述与最终实现存在出入，以
> [README.md](README.md) 为准：实际为**研究 + 质检两个角色**（非三 Agent 状态机）、
> 队列为 ThreadPoolQueue（RQ 为预留）、鉴权为共享 Cookie Token（非 JWT）、
> 数据源为 SEC/arXiv/PubMed/新闻 RSS 六个。
> 整改建议见 [docs/企业认可改法.md](docs/企业认可改法.md)。

> 定位：基于开源内核 [smolagents](https://github.com/huggingface/smolagents)（Hugging Face）的二次开发，
> 打造一个**从数据采集到研报产出、可部署上线的行业 Agent**。
> 面试叙事：复用内核，自研编排（研究/质检双角色）+ 检索与治理 + 可评测 Harness。

---

## 1. 项目目标与可量化成果（写进简历/README 的硬指标）

| 指标 | 目标值 | 证明方式 | 实际状态 |
|---|---|---|---|
| 数据源接入 | ≥ 5 个真实公开数据源 | README 数据源清单 + 采集日志 | ✅ 6 个（SEC/arXiv/PubMed/RSS×3） |
| 研报产出效率 | 人工 2 小时 → Agent ≤15 分钟 | 演示视频计时 | ⚠️ 无线上连续产出计时，勿写死数字 |
| 自动化产出 | 每日日报 + 每周深度研报定时生成 | 线上存档 | ⚠️ 调度代码有，连续产出未跑 1 周 |
| 事实准确性 | 质检校验 + 数字带引用 | 报告引用率统计 | ✅ 质检 + caveat/reject 策略 |
| 服务性能 | 压测报告（QPS/P95/失败率） | 压测报告 | ✅ QPS 1273 / 0 错误（本地） |
| 交付物 | 产品 + 博客 + 评测 | 链接 | ✅ 6 篇博客 + GitHub + 43 测试 |

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│  前端工作台（自研 React + Vite + TS）                         │
│  任务创建 · 流式执行日志(Agent 思考过程可视化) · 研报阅读器 ·    │
│  历史会话 · 数据源状态面板                                    │
└──────────────────────────┬──────────────────────────────────┘
                           │ SSE (EventSource) / REST
┌──────────────────────────▼──────────────────────────────────┐
│  服务层（自研 FastAPI + Celery + Redis + SQLite/Postgres）    │
│  会话管理 · 任务调度 · 鉴权(JWT) · 研报存储 · API 网关         │
└───────┬───────────────────────────────┬─────────────────────┘
┌───────▼─────────────┐        ┌────────▼─────────────────────┐
│  Agent 编排层         │        │  知识层 RAG（自研）           │
│  (smolagents 内核扩展) │        │  文档切分 · 向量化(bge-m3)    │
│  研究 Agent（规划/写作）│        │  Chroma 向量库 · 混合检索     │
│  数据 Agent（查证）    │        │  (BM25+向量) · 重排(可选)     │
│  质检 Agent（事实校验） │        └────────┬─────────────────────┘
└───────┬──────────────┘                 │
┌───────▼────────────────────────────────▼────────────────────┐
│  数据层（自研采集管道，APScheduler 定时）                       │
│  行业新闻(SEMI/SIA/集微网) · 公司财报(SEC EDGAR API) ·          │
│  行情数据(公开接口) · 清洗/去重/入库                            │
└─────────────────────────────────────────────────────────────┘
                    │ Docker Compose 一键部署
        ┌───────────▼────────────┐
        │  云服务器 + Nginx/Caddy + HTTPS + 健康检查  │
        └────────────────────────┘
```

**模块归属（面试时如实说明）**：
- **复用开源**：smolagents 的 Agent/CodeAgent/工具调用内核（只读 + 薄扩展）
- **全部自研**：数据层、RAG、多 Agent 编排策略、服务层、前端、部署

---

## 3. 技术选型表

| 层 | 选型 | 理由 |
|---|---|---|
| Agent 内核 | smolagents（锁定稳定版本） | 代码量小可精读；CodeAgent 让模型写代码调工具，技术叙事有深度 |
| LLM | DeepSeek / 通义 / GLM API（可配置多 Provider） | 便宜、国内可直连；smolagents 支持自定义 Model 接入 |
| 服务层 | FastAPI + uvicorn + sse-starlette | 轻量、异步、流式输出天然支持 |
| 任务队列 | Celery + Redis（起步可先用 RQ） | 长任务（研报生成 10~15 分钟）必须异步化 |
| 定时调度 | APScheduler | 每日数据采集 + 每日/每周自动研报 |
| 向量库 | Chroma（起步）→ pgvector/Milvus（可选升级） | 轻量起步，避免过早复杂度 |
| Embedding | bge-m3（API 或本地） | 中文效果好、便宜 |
| 数据存储 | SQLite 起步 → PostgreSQL | 会话/研报/采集记录 |
| 前端 | React + Vite + TypeScript + Ant Design + ECharts | 生态成熟，流式渲染 + 图表（半导体指数走势） |
| 部署 | Docker Compose + 云服务器（腾讯云/阿里云轻量） + Nginx + Let's Encrypt | 一键部署、学生机即可（2C4G） |
| 测试 | pytest + 集成测试 + GitHub Actions（可选） | 面试加分项 |
| 爬虫 | httpx + BeautifulSoup + 优先官方 API/公开接口 | 尊重 robots.txt，优先 SEC EDGAR 等无鉴权官方 API |

---

## 4. 原创改造任务拆分（= 简历项目经历 + 面试素材）

### M1 数据采集管道（第 3 周）
- [ ] 数据源调研与合规确认：SEMI、SIA、集微网、SEC EDGAR（上市公司 10-K/Q）、行情公开接口
- [ ] 统一采集框架：源适配器模式（每个源一个 Adapter，便于扩展）
- [ ] 清洗/去重/标准化入库（文章、财报关键指标、行情快照三张表）
- [ ] 定时调度 + 失败重试 + 采集状态监控

### M2 RAG 知识库（第 4 周）
- [ ] 文档切分策略（按标题层级 + 滑动窗口，记录切分实验）
- [ ] 向量化入库 + Chroma 持久化
- [ ] 混合检索（BM25 + 向量）与重排，检索效果评测（Recall@k）
- [ ] 引用溯源：每条研报论断自动附带来源链接/段落

### M3 多 Agent 编排（第 4~5 周）
- [ ] 研究 Agent：拆解任务 → 规划大纲 → 分节写作（smolagents CodeAgent 扩展）
- [ ] 数据 Agent：检索知识库 + 实时查证数字（工具：search/calc/query_db）
- [ ] 质检 Agent：交叉核对数字与引用、标记不确定信息、幻觉率统计
- [ ] 编排器：三 Agent 协作状态机 + 失败重试 + 中间产物保存

### M4 研报生成器（第 5 周）
- [ ] 日报/周报/深度研报三套模板
- [ ] Markdown 渲染 + PDF 导出（weasyprint）
- [ ] 报告版本管理与历史对比

### M5 服务层（第 5~6 周）
- [ ] FastAPI：任务创建/查询/取消、会话管理、研报查询
- [ ] SSE 流式输出（Agent 思考过程实时推送到前端）
- [ ] JWT 鉴权 + 用户/任务隔离
- [ ] Celery 任务队列接入 + 任务状态持久化

### M6 前端工作台（第 6~7 周）
- [ ] 任务创建页（选题、周期、深度选项）
- [ ] 流式执行视图（Agent 步骤、工具调用、耗时可视化）
- [ ] 研报阅读器（Markdown 渲染 + 引用跳转 + 图表）
- [ ] 历史会话 + 数据源状态面板

### M7 生产化（第 7 周）
- [ ] Docker Compose 编排全部服务
- [ ] 云服务器部署 + HTTPS + 健康检查 + 日志
- [ ] 接入真实数据连续运行 ≥ 1 周，产出存档
- [ ] 压测报告 + README + 演示视频 + 2 篇技术博客

---

## 5. 8 周时间线

| 周 | 阶段 | 里程碑产出 |
|---|---|---|
| W1 | 读代码 + 搭骨架 | smolagents 精读笔记 + 架构博客① + 项目骨架 |
| W2 | 读代码 + 技术预研 | RAG/SSE/队列技术验证 demo + 博客② |
| W3 | M1 数据管道 | 5 数据源跑通，每日自动采集 |
| W4 | M2 RAG + M3 编排 | 能生成"带引用的初版研报" |
| W5 | M3 完善 + M4 + M5 | 多 Agent 协作稳定产出 + API 可用 |
| W6 | M5 + M6 前端 | 全链路可交互（浏览器里看 Agent 干活） |
| W7 | M7 生产化 | 上线 + 连续运行 + 压测报告 |
| W8 | 收尾 | README/演示视频/博客发布/面试题清单 |

---

## 6. 第一周行动清单（按天）

| 天 | 任务 | 产出 |
|---|---|---|
| D1 | 环境：Python 3.11/3.12 + uv；clone smolagents；跑通官方 quickstart | 本地跑通第一个 Agent |
| D2 | 精读核心源码：`agent.py`、`code_executor.py`、`tools`、`models` | 类图 + 执行流程图 |
| D3 | 精读多 Agent 与 tool 机制；写 2 个自定义 Tool（web_search、db_query） | 自定义工具 demo |
| D4 | 架构博客①：smolagents 内核剖析（发掘真 + 掘金） | 博客链接 |
| D5 | 搭项目骨架：monorepo（backend/frontend/data/agent）+ git + CI 基础 | GitHub 仓库 |
| D6 | 调研并验证 2 个数据源（SEC EDGAR API + 行业新闻站） | 采集脚本原型 |
| D7 | 周复盘：更新 PLAN.md 状态，确定 W2 计划 | 复盘记录 |

---

## 7. 面试准备（诚实叙事话术 + 深挖问题清单）

**自我介绍话术**：
> "这是一个基于 Hugging Face smolagents 内核二次开发的半导体行业研报生成系统。
> 我负责了数据采集管道、RAG 知识库、多 Agent 编排（研究/数据/质检）、FastAPI 服务层和前端工作台的完整设计实现，并已部署上线、连续运行。对比人工，一份行业周报的产出时间从约 2 小时缩短到 15 分钟。"

**必背深挖问题**（W8 前逐一准备答案）：
1. 为什么选 smolagents 的 CodeAgent 而不是 ReAct 结构？
2. 代码执行的安全性怎么处理？（沙箱/限制）
3. RAG 切分策略怎么定的？为什么混合检索？
4. 怎么保证研报事实准确？质检 Agent 的校验逻辑是什么？
5. 任务队列为什么用 Celery？如果 worker 挂了怎么恢复？
6. SSE 流式输出怎么实现的？断线重连呢？
7. 多 Agent 之间怎么协作？状态机怎么设计？失败重试策略？
8. 数据源反爬/挂了怎么办？数据质量怎么保证？
9. LLM 成本和延迟怎么控制？（缓存、模型分级）
10. 如果让你扩展成"财报智能解读"，架构上要改什么？

---

## 8. 风险与备选方案

| 风险 | 应对 |
|---|---|
| LLM API 成本 | 用 DeepSeek 等低价 API；结果缓存；模型分级（规划用强模型、检索用轻模型） |
| 爬虫合规/反爬 | 优先官方 API（SEC EDGAR 免费无鉴权）；尊重 robots.txt；失败重试 + 降级 |
| 时间不足 | 砍单：质检 Agent 降级为规则校验；前端用模板渲染；PDF 导出延后 |
| smolagents 升级破坏兼容 | 锁定版本号 + 记录改动 diff |
| 研报质量不稳定 | 模板约束 + 质检 Agent 把关 + 失败重试 + 人工抽样评估 |
| 面试被追问"哪些是你写的" | 如实分层：内核复用、其余自研；每层都有代码/提交记录可展示 |

---

## 9. 参考资源

- smolagents 官方：https://github.com/huggingface/smolagents · https://smolagents.org
- OpenManus 架构（参考读物）：https://github.com/kumanday/OpenManus/blob/main/architecture.md
- 框架对比：https://zhuanlan.zhihu.com/p/1984725399767376336
- SEC EDGAR API（财报数据源）：https://www.sec.gov/edgar/sec-api-documentation
- 半导体行业数据：SEMI https://www.semi.org · SIA https://www.semiconductors.org
